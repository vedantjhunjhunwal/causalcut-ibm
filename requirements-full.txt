# Full model stack, on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-full.txt

# --- Tabular ML (gas XGBoost + IsolationForest, AI4I LightGBM, hydraulic) ---
numpy>=1.26.4
pandas>=2.2.3
scipy>=1.14.1
scikit-learn>=1.5.2
joblib>=1.4.2
xgboost>=2.1.3
lightgbm>=4.5.0

# --- Regulatory RAG -------------------------------------------------------
faiss-cpu>=1.9.0
sentence-transformers>=3.3.1

# --- Vision (YOLOv8) + tracking (ByteTrack) -------------------------------
torch>=2.5.1
ultralytics>=8.3.49
supervision>=0.24.0
lapx>=0.5.11

# --- Agentic AI (sensor drift detection via ADWIN) -------------------------
river>=0.21
